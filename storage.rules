rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /ovpn/{userId}/{fileName} {
      allow read, write: if false;
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
