rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-only access via Firebase Admin SDK (Cloud Functions / local backend).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
